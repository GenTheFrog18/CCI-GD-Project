# Imported map updates

These scenes are additive copies from the external map branches. Existing
world layers and sections remain unchanged.

- `level_design/`: scenes from `map/level-design`.
- `surface_rebuild_story_radar/`: the rebuilt surface scene from
  `feature/surface-rebuild-story-radar`.

The story-radar branch's objective HUD, objective box, radar arrows, and
related gameplay/UI code were intentionally not imported.
